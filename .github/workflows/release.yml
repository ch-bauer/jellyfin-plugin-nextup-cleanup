name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

jobs:
  release:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - uses: actions/setup-dotnet@v6
        with:
          dotnet-version: 9.0.x

      - name: Test
        run: dotnet test tests/Jellyfin.Plugin.NextUpCleanup.Tests

      - name: Build
        run: dotnet publish src/Jellyfin.Plugin.NextUpCleanup -c Release -o publish

      - name: Package
        run: |
          VERSION="${GITHUB_REF_NAME#v}"
          mkdir package
          cp publish/Jellyfin.Plugin.NextUpCleanup.dll package/
          cp meta.json package/
          (cd package && zip "../nextup-cleanup_${VERSION}.zip" ./*)
          echo "MD5 checksum for manifest.json:"
          md5sum "nextup-cleanup_${VERSION}.zip"

      - name: Create GitHub release
        uses: softprops/action-gh-release@v3
        with:
          files: nextup-cleanup_*.zip
          generate_release_notes: true
